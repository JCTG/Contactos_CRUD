<%= form_for(libretum) do |f| %>
  <% if libretum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(libretum.errors.count, "error") %> prohibited this libretum from being saved:</h2>

      <ul>
      <% libretum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :Telefono %>
    <%= f.number_field :tlf %>
  </div>

   <div class="field">
    <%= f.label :Correo %>
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
